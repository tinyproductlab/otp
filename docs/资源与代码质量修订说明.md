# 小产品实验室 OTP｜资源与代码质量修订说明

**修订日期：** 2026-08-25  
**适用工程：** 小产品实验室 OTP

## 一、问题定位

工程的运行时图片资源本身没有超过 200 KB：`assets/otp-logo.jpg` 为 34,143 字节（约 33.3 KB）。但项目根目录的 `docs/` 中包含开发联调截图，大小为 1,351,597 字节（约 1.29 MB），并且此前未被打包忽略规则排除。该截图不属于小程序运行资源，却可能随项目文件被质量检查扫描，从而触发图片资源大小限制。

| 资源类别 | 修订前 | 修订后 |
|---|---|---|
| 运行时图片 | `assets/otp-logo.jpg`，约 33.3 KB | 保留，低于 200 KB。 |
| 运行时音频 | 无 | 无。 |
| 联调截图和交付文件 | 位于 `docs/`，其中截图约 1.29 MB，未显式忽略 | `docs/` 已作为文件夹加入打包忽略规则。 |
| 未引用根目录图片 | 约 111.7 KB，不被页面引用 | 已作为单文件加入打包忽略规则。 |

## 二、已实施修复

已更新 `project.config.json` 的 `packOptions.ignore`。运行时不需要的 `docs/`、`参考资料/`、测试目录、方案文档、交付说明、待办文件和未引用根目录图片不会再进入小程序包或资源质量检查范围。

同时完成静态代码质量复核。运行时代码未发现 `console.*`、`debugger`、`TODO/FIXME`、`eval`、`new Function` 或空源文件；定时器仅用于 OTP 倒计时与验证码预览，并与对应页面生命周期清理逻辑配套。页面与数据层测试覆盖了导入导出、备份口令、TOTP、密码账本、生成器和离线安全检查等关键路径。

## 三、复核结果

| 验证项 | 结果 |
|---|---|
| 运行时图片与音频均不超过 200 KB | 通过。唯一运行时图片为约 33.3 KB 的 `assets/otp-logo.jpg`。 |
| 非运行时大截图不进入小程序包 | 通过。`docs/` 已被忽略。 |
| 小程序模拟器编译日志 | 未发现 WXML、WXSS、JavaScript 语法或“编译失败”错误。 |
| 自动化回归 | AES 21 项、哈希 42 项、TOTP 63 项、密码生成 41 项、数据仓库 111 项、页面 122 项、安全行动计划 16 项、可疑检查 20 项、密码策略 11 项均通过。 |
| 静态调试代码检查 | 通过。未发现调试输出、动态执行或未完成标记。 |

> 重新上传或提交代码会触发平台最终的远端质量检查。本次修订没有上传、提交或发布任何版本。

作者：Manus AI。
