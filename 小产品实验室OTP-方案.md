# 小产品实验室 OTP — 功能边界与页面方案

> 视觉与信息架构基准:`/Users/eve/移动端开发 ios-andriod/KeyScan-Android`
> iOS 工程不作参考(SwiftUI + 单文件 JSON 存储,无可迁移部分)
> 拟定日期:2026-08-20

---

## 一、功能范围

### 做

| 模块 | 说明 |
|---|---|
| TOTP 认证器 | 扫码添加(`otpauth://`)、手动添加、30 秒刷新、点击复制 |
| 密码账本 | 静态保留(归档定位),**无自动填充**,分组、搜索、排序 |
| 密码生成器 | 长度 / 大小写 / 数字 / 符号,生成历史 |
| WebDAV 同步 | **预置服务商**,界面注明"仅支持 XX",端到端加密后直传 |
| 离线加密导出 | 兜底通道,同时服务自建服务器用户 |
| 回收站 | 保留期 7/30/90/永久 |
| 设置 | 主题、语言、锁定、清除 |

### 不做(平台不允许,非取舍)

| 项 | 原因 |
|---|---|
| 自动填充 | 小程序无系统级集成,不存在此能力 |
| **局域网互传** | 小程序**不能监听端口/当服务端**,`LanBackupTransfer` 的模型无法成立;且无后台执行,切前台即断 |
| 任意地址 WebDAV | 域名必须发布期登记,运行时用户输入的地址无法访问 |
| OCR / 智能扫描 | 无对应能力(安卓侧 `OcrScanFragment` 958 行不迁移) |
| 附件保险箱 | 存储上限 10MB/用户,附件放不下 |
| 剪贴板定时清空 | 无后台定时器(安卓 `ClipboardGuard` 那套搬不过来) |
| 自动锁定 / 自动备份 | 同上；不提供本地文件夹自动备份，用户可手动导出至微信或使用 WebDAV |

> ⚠️ **局域网互传是你原始清单里的一项,这里被移除了。** 不是我替你砍的,是平台封的。若这项是硬需求,小程序这个载体本身就要重新考虑。

---

## 二、页面结构(对照安卓)

安卓是**三页横向滑动 + 底部三栏**(`fragment_primary_navigation.xml`),小程序用 `swiper` + 自定义 `tabBar` 可 1:1 复刻。

```
底部三栏:  实用工具  |  首页  |  数据
```

### 首页 ← `primary_page_home.xml`

核心是**三张叠放卡**(安卓 `home_core_stack_height=304dp`,三卡 `marginTop` 0/72/144dp,`elevation` 3/5/7 递增形成堆叠):

| 安卓卡 | 小程序 | 说明 |
|---|---|---|
| TOTP认证器 | ✅ 保留 | `home_otp` |
| 安全保险箱 | ⚠️ **待定** | `home_password_notes` — 见第六节 |
| 密码账本 | ✅ 保留 | `home_password_forge` |

下方**安全状态卡**(`security_status_title`「安全状态」)三行:生物识别 / 加密状态 / 重复密码检查。小程序版建议改为:锁定状态 / 加密状态 / 弱密码检查。

顶部:品牌标识 + 微信导入导出入口 + 云同步按钮(绿=已配置 / 橙=待同步)。

### 数据页 ← `primary_page_data.xml`

| 入口 | 安卓 id | 小程序 |
|---|---|---|
| WebDAV 备份 | `primary_data_backup` | ✅ 改为服务商选择 |
| 回收站 | `primary_data_trash` | ✅ |
| 历史记录 | `primary_data_history` | ⚠️ 扫描历史随扫描功能缩减,建议改为"生成历史" |

### 实用工具页 ← `primary_page_tools.xml`

安卓只有一项 `primary_tool_scan`(扫一扫)。小程序保留,但**定位从"扫描工具"改为"录入通道"**:扫 `otpauth://` 加 TOTP、扫 WebDAV 配置码。用 `wx.scanCode` 原生 API,比安卓自己搓 CameraX+MLKit 省事得多。

### 列表项组件

直接对照,一对一转 WXML 组件:

| 安卓 layout | 行数 | 结构要点 |
|---|---|---|
| `item_otp_token.xml` | 133 | 高 64dp:品牌图标 38dp / 发行商 12sp + 账号 16sp 粗 / 验证码区 86dp(21sp 粗,色 `primary` #4A90D9,下方「点击复制」10sp)/ 倒计时 chip 40×26 + 进度条 38×4 |
| `item_password_entry.xml` | 103 | 头像 + 标题 + 账号 + 安全检查标记 |
| `item_password_group.xml` | 81 | 组名 + 描述 + N 条 + 更多 |
| `item_password_generation_record.xml` | 87 | 生成历史行 |

---

## 三、视觉规范

安卓 `values/colors.xml`(93 行)+ `values-night/colors.xml`(91 行)直接转 WXSS 变量,深浅双主题一次做对。

```css
/* 亮色 → 暗色 */
--primary:        #4A90D9  →  #8AB4F8   /* 主色 / TOTP 验证码色 */
--accent:         #2EAF9A  →  #2DD4BF
--danger:         #D93025  →  #D93025
--success:        #188038  →  #81C995
--text-main:      #334155  →  #E2E8F0
--text-secondary: #64748B  →  #94A3B8
--text-hint:      #94A3B8  →  #64748B
--card-bg:        #FFFFFF  →  #1E293B
--card-stroke:    #E8EDF2  →  #334155
--page-bg:        #FFFFFF  →  #0F172A
--settings-bg:    #F4F7FC  →  #08111F
--ledger-bg:      #F5F8FC  →  #06101F
```

间距取 `values/dimens.xml`:首页上下留白 10/20dp、卡片圆角与内距参考 `password_ledger_styles.xml` / `data_management_styles.xml`。

**文案不用重写** — `values/strings*.xml` 共 60 个文件、733 条,直接取用。重点:`strings_otp.xml`、`strings_password.xml`、`strings_trash.xml`、`strings_primary_*.xml`。

---

## 四、数据与存储

```
wx.setStorage({ encrypt: true })   // 微信托管密钥,绑 openid + 设备
```

**容量:10MB / 用户 / 小程序。** 纯文本凭据几百条约几百 KB,够用;附件不可行。

数据模型可沿用安卓 `data/model/` 的字段,但**去掉自动填充专用字段**:
- `PasswordEntry.websiteDomain` / `appPackageName` — 原本用于填充匹配,现降级为纯展示,建议合并为单个 `site` 字段

> ⚠️ **`encrypt: true` 的密钥由微信托管、绑定设备。** 换设备、清缓存、掉登录态都有丢数据风险。这意味着**导出通道不是附属功能,而是数据安全的唯一保险**,首次使用就应引导用户导出一次。

---

## 五、同步设计

### WebDAV(预置服务商)

界面从安卓的"填地址"改为"选服务商":

```
安卓  fragment_webdav_config.xml:  [地址输入框] [用户名] [密码]
小程序:                            [服务商 ▾]  [账号]  [应用密码]
                                   提示:仅支持 XX / XX,不支持自建服务器
```

后台"服务器域名"登记固定几家的 DAV 域名即可。国内用户想要 WebDAV 的绝大多数用的就是坚果云(支持**应用密码**,用户不必交出主密码),体验基本无损。

**动词限制与绕法**(安卓 `WebDAVClient.java` 实际用 5 个动词):

| 动词 | 安卓用途 | 小程序 | 方案 |
|---|---|---|---|
| PUT | 上传备份 | ✅ | 直接用 |
| GET | 下载备份 | ✅ | 直接用 |
| DELETE | 删除旧备份 | ✅ | 直接用 |
| MKCOL | 建目录 | ❌ | 要求用户预先建好目录 |
| PROPFIND | 测连接 / 列备份 | ❌ | 自维护 `index.json` 清单:PUT 备份时同步更新;测连接改为 GET 探针文件 |

数据**端到端加密后直传用户自己的网盘,不经过任何中转** — "自主可控"这个卖点保住了。

### 离线加密导出(兜底)

生成加密备份 → 文件 / 二维码 → 用户自存。同时服务自建 NAS 用户(他们导出后自己上传)。

### ⚠️ 加密实现的两个坑

1. **`crypto-js` 不支持 AES-GCM。** 安卓备份格式(KSB5/KSB6)用的是 AES-GCM,纯 JS 要 GCM 得换 `sjcl` 或自己接。
2. **PBKDF2 240k 迭代在纯 JS 里会卡几秒。** 三个选择:放进小程序 **Worker**(推荐)/ 降迭代数(削弱安全,不推荐)/ 放弃与安卓格式兼容,自己定一套。

**建议:不追求与安卓 KSB5/KSB6 字节级兼容**,自己定格式 + 跑在 Worker 里。跨端互导可以后面用"导出 JSON 再导入"这种粗粒度方式解决,别把两端的二进制格式绑死 —— 安卓那边已经为此在 iOS 侧手写复刻了一整套 `AndroidBackupCrypto`,不值得再来一次。

---

## 六、待你确认的三件事

1. **安全保险箱要不要?** 你的清单里没提,但安卓首页三张核心卡它占一张。考虑到附件在小程序里放不下,没有附件的保险箱其实就是"另一种条目类型"。**我的默认假设:不做独立保险箱**,若需存静态资料就并入密码账本作为条目类型 —— 首页因此是两张核心卡。要保留请说。
2. **历史记录页留什么?** 扫描功能缩减为录入通道后,"扫描历史"意义不大。建议改为"生成历史"(密码生成记录)。
3. **预置哪几家 WebDAV?** 至少坚果云。要不要再加 TeraCloud / InfiniCLOUD 等。

---

## 七、开工前的前置风险

**先去小程序后台确认类目能不能过,再写代码。**

微信对"托管用户账号密码"这类工具的类目和资质要求可能卡人,个人主体尤其。这是**会导致白干**的风险,优先级高于任何技术决策。我无法替你确认具体条款,但建议在动手前先把这件事落实。
