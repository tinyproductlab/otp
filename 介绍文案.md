# 小产品实验室 OTP — 介绍文案

按用途分好了,直接复制。所有描述都与已实现的功能一致,没有超出实际能力的表述。

> 名字里带 OTP,定位就前移了:**这是一个验证码工具,顺便带了密码账本**,而不是反过来。
> 下面所有文案都按这个主次写。

---

## 一、一句话定位

> **验证码在自己手机上算,不联网。**

备选:

- 本地计算的两步验证码工具,顺带保管密码
- 不用注册、不上传、不联网的 TOTP 验证器
- 你的两步验证码,只存在这台手机上

---

## 二、小程序后台「服务简介」

微信后台的服务简介有字数限制(约 120 字以内),给三个长度:

**短版(36 字)**

> 本地计算的两步验证码(TOTP)工具,附带密码账本和密码生成器。数据只存在你的手机上。

**中版(74 字)**

> 扫码添加两步验证码,验证码在本机按标准算法计算,全程不联网。附带密码账本、随机密码生成器和回收站。数据加密存放在本机,可导出备份或同步到你自己的坚果云。

**长版(116 字)**

> 小产品实验室 OTP 是一个本地优先的两步验证码工具:扫一下网站的二维码就能添加,验证码在你的手机上计算,不需要联网。同时提供密码账本用于静态保管账号密码,以及随机密码生成器和回收站。数据加密存放在本机,不上传任何服务器,可导出加密备份或同步到自己的坚果云账号。

---

## 三、介绍页 / 帮助页正文

### 小产品实验室 OTP

**验证码在自己手机上算,不联网。**

这是一个两步验证码(TOTP)工具,顺带帮你保管账号密码。所有数据加密存在你这台手机的微信本地存储里 —— 我们没有服务器,也就没有可以泄露的数据库。

---

#### 能做什么

**两步验证码(TOTP)**
扫一下网站「双重验证 / 两步验证」页面的二维码就加进来了,也可以手动输入密钥。验证码在你的手机上按 RFC 6238 标准算出来,全程不联网。点一下即复制,顺便告诉你还剩几秒失效。支持 6/8 位、30/60 秒周期、SHA1/SHA256。

**密码账本**
账号、密码、网站、备注,分组管理,支持搜索和排序。弱密码和重复密码会自动标出来。定位是**静态保管** —— 不自动填充,需要时手动复制。

**密码生成器**
4 到 64 位任意长度,大小写、数字、符号自由组合。需要手抄密码时,可以排除 `0O`、`1I`、`l`、`o` 这些形近字符。生成过的密码会留在历史里,一时没保存也找得回来。

**回收站**
删掉的东西先进回收站,保留期可以设成 7 天、30 天、90 天或永久。误删有救。

**备份**
三条路,任选:
- 导出成加密文件,转发到微信收藏或任何地方
- 复制成一段加密文本,粘到你的笔记里
- 同步到你自己的坚果云账号

---

#### 关于安全

**数据在哪** — 只在你这台手机的微信本地存储里,开启了加密存储。没有服务器,不收集任何信息。

**备份怎么加密** — AES-256-GCM,密钥由你的备份密码经 120,000 次 PBKDF2-SHA256 派生。备份文件带完整性校验,被改一个字节就打不开。上传到坚果云的是密文,坚果云看不到内容。

**备份密码只在你脑子里** — 我们不保存、不上传、也无法帮你找回。忘了就真的打不开了。

---

#### ⚠️ 请一定看这一条

**微信本地存储的加密密钥由微信托管,并且和你的设备绑定。**

这意味着:换手机、清理微信缓存、长期不用被系统清理,都可能导致本机数据无法解密。

**所以导出备份不是可选项,是唯一的保险。** 建议第一次录入数据后就导出一次,之后定期导出。

对验证码来说这条尤其重要 —— 如果本机数据丢了而你又没有备份,那些开了两步验证的账号可能会登不进去。请确认每个账号都留有其他验证方式(备用码、短信等)。

---

#### 有意不做的功能

一个工具说清楚自己不做什么,比堆功能更有用:

| 不做 | 原因 |
|---|---|
| 自动填充密码 | 微信小程序没有系统级填充能力,做不到 |
| 局域网互传 | 小程序不能监听端口、不能当服务端,做不到 |
| 填任意 WebDAV 地址 | 小程序访问的域名必须在发布前登记,只能预置坚果云 |
| 自动定时备份 | 小程序没有后台运行能力,只能你手动点 |
| 复制后自动清空剪贴板 | 同上,没有后台定时器。只能在复制时提醒你尽快粘贴 |
| 存附件、证件照 | 单用户存储上限 10MB,放不下 |

如果你需要自动填充、自建服务器同步或者附件保管箱,请用 Android 版。

---

## 四、分享 / 推荐文案

**微信分享标题(适合转发卡片)**

> 验证码在自己手机上算,不联网

**朋友圈 / 群聊口语版**

> 找了一圈两步验证的工具,大部分都要注册账号、把数据传到人家服务器。这个不用登录、不联网,验证码在自己手机上算,想备份就导出到自己的坚果云。够用。

**给技术朋友的一句话**

> 本地优先的 TOTP 小程序,顺带密码账本。无服务器无账号,RFC 6238 标准实现,备份 AES-256-GCM + PBKDF2 120k,同步走你自己的坚果云 WebDAV。明确不做自动填充(小程序没这能力)。

---

## 五、关于页文案

```
小产品实验室 OTP
版本 1.0.0

验证码在自己手机上算,不联网。

· 数据只存放在本机,不上传服务器
· 验证码按 RFC 6238 标准在本地计算
· 备份采用 AES-256-GCM 加密
· 备份密码由你保管,我们无法找回
· 请定期导出备份 —— 换机或清缓存会导致本机数据无法解密

© 2026 小产品实验室
```

---

## 六、写文案时的几条自律

给之后维护文案的人:

1. **不要写"绝对安全""军工级加密""永不泄露"。** 既不诚实,也容易在审核时被质疑。说清楚用了什么算法、数据在哪,比形容词有力。
2. **不要暗示可以替代密码管理器。** 没有自动填充,定位就是"保管 + 手动复制",说明白反而少一批失望的用户。
3. **丢数据的风险必须写在显眼处。** 微信存储密钥绑设备这件事,用户不会自己知道,事后发现会觉得被坑。验证码场景后果更重,要额外提醒留备用验证方式。
4. **不要承诺"自动同步"。** 小程序没有后台能力,所有同步都是手动触发,文案里别用"自动"这个词。
5. **坚果云的限制要提。** 免费版有流量和请求频率限制,以及需要用户自己先建好文件夹 —— 这两件事不提前说,用户会以为是小程序坏了。
6. **主次别倒。** 名字里是 OTP,验证码是主角。介绍时先说验证码,密码账本作为附带能力提及。

---

## 七、命名相关的待确认项

**小程序名称需要在后台提交审核,可能被驳回。** 常见原因是重名、或名称中含有被限制的词。「实验室」这类带机构意味的词,不同主体类型(个人 / 企业)的限制不一样 —— 建议在后台先走一次名称检测,确认能过再做物料。

如果需要备用名,可以考虑保留「OTP」这个功能标识不动,只换前缀。
